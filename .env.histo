# =============================================================================
# HISTO-CYTO LAB REPORT SYSTEM - ENVIRONMENT CONFIGURATION
# =============================================================================

# --- Port Configurations ---
# Database Ports (Host side)
HISTO_USERS_DB_PORT=5450
HISTO_PATIENTS_DB_PORT=5451
HISTO_REPORTS_DB_PORT=5452
HISTO_SIGNATURES_DB_PORT=5453

# Application Ports (Host side)
HISTO_BACKEND_PORT=8001
HISTO_FRONTEND_PORT=3001

# --- PostgreSQL Configuration ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_PORT=5432

# Database Hosts (Docker internal names)
POSTGRES_HOST_HISTO_USERS=db-histo-users
POSTGRES_HOST_HISTO_PATIENTS=db-histo-patients
POSTGRES_HOST_HISTO_REPORTS=db-histo-reports
POSTGRES_HOST_HISTO_SIGNATURES=db-histo-signatures

# Database Names
POSTGRES_DB_HISTO_USERS=histo_users
POSTGRES_DB_HISTO_PATIENTS=histo_patients
POSTGRES_DB_HISTO_REPORTS=histo_reports
POSTGRES_DB_HISTO_SIGNATURES=histo_signatures

# --- Security & Authentication ---
SECRET_KEY=histo-cyto-secret-key-change-this-in-production-please-make-it-secure
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# --- AI Integration (OpenAI) ---
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# --- CORS Configuration ---
CORS_ORIGINS=*

# --- Application Configuration ---
ENVIRONMENT=development
PROJECT_NAME=HistoCyto Lab System
API_VERSION=v1

# --- Frontend Configuration ---
PROTOCOL=http
HOST_NAME=localhost
NEXT_PUBLIC_USER_COOKIE=histo_auth_token

# API URLs
API_URL=http://localhost:8001
BACKEND_URL=http://localhost:8001
